spring:
  application:
    name: brownie
  ai:
    anthropic:
      api-key: ${ANTHROPIC_API_KEY:}

telegram:
  bot:
    token: ${TELEGRAM_BOT_TOKEN:}
    username: nathan-brownie
    mode: ${TELEGRAM_BOT_MODE:long-polling}
    webhook:
      url: ${TELEGRAM_WEBHOOK_URL:}
      path: /telegram/webhook
